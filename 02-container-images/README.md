# Imagenes de contenedor FreshBox

Los Dockerfiles viven junto al codigo fuente:

- `../microservicioFrontend/`
- `../microserviciosBackend/{get,create,update,delete}-product/`

Build/push: `../03-deployment-scripts/push-images-to-ecr.sh` (plataforma `linux/arm64` para `t4g.small`).
